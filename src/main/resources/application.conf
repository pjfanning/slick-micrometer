slick {
  dbs {
    default {
      profile = "slick.jdbc.H2Profile$"
      db {
        driver = "org.h2.Driver"
        url = "jdbc:h2:mem:slick;DB_CLOSE_DELAY=-1"
        maxConnections = 20
      }
    }
  }
}